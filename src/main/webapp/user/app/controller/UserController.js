Ext.define("AM.controller.UserController",{
	extend : "Ext.app.Controller",
	views : ["UserList"],
	models : ["UserModel"],
	stores : ["UserStore"],
	init : function(){
		this.control({
			
		});
	}
});